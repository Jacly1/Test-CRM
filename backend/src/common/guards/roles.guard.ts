// Deprecated: replaced by PermissionsGuard. Re-exported (also under the old
// name) so any remaining import resolves without changes.
export { PermissionsGuard, PermissionsGuard as RolesGuard } from './permissions.guard';
